# Pega el contenido de este archivo en el panel de Secrets de Streamlit Cloud:
#   share.streamlit.io → tu app → tres puntos (⋮) → Settings → Secrets
#
# Sin esta key el app funciona normal para US stocks via EDGAR.
# Con la key los stocks internacionales (DAX, Nifty, LatAm) tambien se enriquecen.
# Key gratuita: https://financialmodelingprep.com/developer/docs

FMP_API_KEY = "your_free_api_key_here"
